# End-to-end browser tests. These drive a real Chromium against a live server,
# so they are slower than the rest of the suite and skip themselves cleanly when
# Playwright or its browser binary is not installed.
