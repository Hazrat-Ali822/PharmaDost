# Sehatyar — extra dependencies for building the local Windows desktop .exe
# (PyInstaller). Not needed to run the server. Windows only.
# Install with:  pip install -r requirements-build.txt

-r requirements.txt

# The QR code on the "Connect a Device" page, so clinic staff scan the LAN address
# instead of typing http://192.168.x.x:8000 into a phone. Optional at runtime —
# without it the page still shows the address in large type.
qrcode==8.0

pyinstaller==6.10.0
pyinstaller-hooks-contrib==2026.6
altgraph==0.17.5
packaging==26.2
pefile==2024.8.26
pywin32-ctypes==0.2.3
