# AGENTS.md (moved)

This file is no longer maintained — its contents had gone out of date and in places
became wrong (it described production as running on Supabase Postgres when it actually
runs on local SQLite, and referenced a `pk=1` SiteSettings fallback that was removed).

The current guide lives in **[CLAUDE.md](../CLAUDE.md)** at the project root.
Read that instead, and keep it updated rather than reviving this file.
